# Deploy in minutes

## GitHub Pages
Upload all files in this folder to a repository and enable Pages for the branch/root.

## Vercel
Create a new project from the repository. Framework preset: Other. Build command: leave empty. Output directory: `.`.

## Cloudflare Pages
Connect the repository. Framework preset: None. Build command: leave empty. Output directory: `.`.

The MVP has no backend and no database.
